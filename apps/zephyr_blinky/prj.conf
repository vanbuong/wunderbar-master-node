CONFIG_GPIO=y
CONFIG_BUILD_OUTPUT_HEX=y
CONFIG_BUILD_OUTPUT_BIN=y

# Zephyr's kernel is the scheduler. CONFIG_MULTITHREADING is on by default.
# This blinky uses a single main thread with k_msleep().
